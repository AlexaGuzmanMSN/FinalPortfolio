@model IEnumerable<FinalPortfolio3.Models.Description>

@{
    ViewBag.Title = "Project Descriptions";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProjectDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Screenshot.Screenshot1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProjectDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Screenshot.Screenshot1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProjectDescriptionID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProjectDescriptionID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProjectDescriptionID })
        </td>
    </tr>
}

</table>
