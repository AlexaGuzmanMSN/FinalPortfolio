@model IEnumerable<FinalPortfolio3.Models.Screenshot>

@{
    ViewBag.Title = "Screenshots";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Screenshot1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Project.ProjectName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Screenshot1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Project.ProjectName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ScreenshotID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ScreenshotID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ScreenshotID })
        </td>
    </tr>
}

</table>
